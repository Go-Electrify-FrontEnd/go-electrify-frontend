"use client";

/* DEPRECATED SHIM
	 This file was left as a compatibility shim during the feature refactor.
	 The source-of-truth has moved to:
		 @/features/connector-type/components/connector-type-actions
	 Remove this shim after verifying there are no remaining imports that
	 reference this path (the repo contains .bak backups next to modified files).
*/
export * from "@/features/connector-type/components/connector-type-actions";
